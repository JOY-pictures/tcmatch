TCMatch — Telegram Freelance Exchange Platform Backend

TCMatch — это бэкенд для телеграм-бота, реализующего функционал биржи фриланса с системой безопасных сделок (Escrow).

Проект демонстрирует реализацию сложной бизнес-логики, управление транзакциями, интеграцию с внешними сервисами и использование конечных автоматов состояний (FSM).

🛠 Технологический стек

Core: Java 17, Spring Boot 3

Database: Spring Data JPA, H2 (для Dev-среды), PostgreSQL (для Prod-среды)

Payments: Интеграция с YooMoney (Webhooks для подтверждения оплаты)

Architecture: Finite State Machine (FSM) для навигации пользователя

Security & Ops: Конфигурация SSL, Graceful Shutdown, разделение конфигураций на публичные и приватные.

💼 Ключевые архитектурные решения

1. Архитектура "Безопасная сделка" (Escrow)

Реализован полный и атомарный цикл финансовой сделки с высоким уровнем надежности:

Схема: Разделение бизнес-сущностей (Application, Escrow, Order) для обеспечения целостности данных.

Атомарность: Гарантия, что финальный Order создается только после получения и проверки Webhook-уведомления об успешной оплате Escrow.

2. Управление состоянием (FSM)

Для управления сложными многошаговыми сценариями (например, пошаговое создание заявки или публикация отзыва) используется паттерн Finite State Machine. Это позволяет:

Устранить путаницу в диалогах пользователя.

Сохранить контекст даже при перезапуске сервера.

3. Производственная готовность (Production-Ready)

Проект сконфигурирован для развертывания на реальном сервере:

Внедрен Graceful Shutdown для безопасного завершения работы без потери текущих транзакций.

Настроен SSL и работа с keystore для обеспечения безопасного HTTPS-соединения, необходимого для приема Webhooks.

🚀 Как запустить локально

Для запуска используется двухфайловая конфигурация, которая разделяет публичные (для Git) и секретные (только локальные) данные.

1. Безопасная настройка конфигурации

Профили: В публичном application.yml заданы общие настройки и заглушки.

Секреты: Все ваши реальные токены и пароли хранятся в файле application-local.yml, который надежно игнорируется Git с помощью .gitignore.

Действия:

Клонируйте репозиторий:

git clone [https://github.com/your-username/tcmatch.git](https://github.com/your-username/tcmatch.git)


Создайте файл секретов: В папке src/main/resources создайте файл application-local.yml и вставьте в него ваши реальные рабочие ключи, чтобы переопределить заглушки:

# --- СЕКРЕТНЫЕ НАСТРОЙКИ (Игнорируются Git) ---

admin:
  super-admin-ids: 5519912522 # Ваш реальный Chat ID

server:
  # Настройки для SSL/Cloudpub (если не используете, можно закомментировать)
  ssl:
    key-store-password: u07... # Ваш реальный пароль

telegram:
  bot:
    token: 7984996902:AAFJo3b... # Ваш настоящий токен

yoomoney:
  shopId: 1218176
  secretKey: test_BSMXFx9KeUJjYTZdO... # Ваш тестовый ключ


2. Запуск

Запустите класс TcmatchApplication.java в вашей IDE. Проект запустится в dev профиле, используя базу данных H2 в памяти для простого тестирования.

📞 Контакты

Если вас заинтересовал мой опыт разработки сложных бэкенд-систем на Spring Boot:
https://t.me/Profity12
