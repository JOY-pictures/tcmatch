TCMatch — Telegram Freelance Exchange Platform

TCMatch — это бэкенд для телеграм-бота, реализующего функционал биржи фриланса с безопасными сделками (Escrow).

Проект демонстрирует реализацию сложной бизнес-логики внутри мессенджера, управление транзакциями и конечными автоматами состояний (FSM).

🛠 Технологический стек

Core: Java 17, Spring Boot 3

Database: PostgreSQL (ранее Firestore), Spring Data JPA

Payments: Интеграция с YooMoney (Webhooks)

Architecture: Finite State Machine (FSM) для навигации пользователя

Deployment: Docker (PostgreSQL)

💼 Реализованный функционал

1. Архитектура "Безопасная сделка" (Escrow)

Реализован полный цикл сделки между Заказчиком и Фрилансером:

Создание заявки: Заказчик публикует Job Post (сохраняется в БД).

Отклики: Фрилансеры отправляют свои предложения.

Инициация Escrow: При принятии отклика создается сущность Escrow, средства "замораживаются" (имитация холда).

Order: После подтверждения оплаты автоматически создается активный Order.

2. Finite State Machine (FSM)

Для управления сложным диалогом (пошаговое создание заявки, ввод цен, описания) используется паттерн FSM. Состояние пользователя (UserSession) хранится в базе данных, что гарантирует сохранность контекста при перезагрузке сервера.

3. База данных и Сущности

Спроектирована реляционная схема данных:

UserSession — хранение контекста и стейта.

Application — заявки на работу.

Escrow — финансовые транзакции.

Order — активные заказы.

🚀 Как запустить локально

Клонируйте репозиторий:

git clone [https://github.com/your-username/tcmatch.git](https://github.com/your-username/tcmatch.git)


Запустите PostgreSQL в Docker:

docker run --name tcmatch-postgres -e POSTGRES_PASSWORD=pass -e POSTGRES_DB=tcmatch_db -p 5432:5432 -d postgres


Настройте переменные окружения:
В настройках запуска IDE укажите:

BOT_TOKEN=ваш_токен
DB_PASSWORD=pass


Запустите TcmatchApplication.java.
