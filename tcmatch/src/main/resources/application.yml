admin:
  enabled: true
  super-admin-ids: 5519912522  # Ваш chatId

  # Опционально: настройки уведомлений
  notifications:
    verification: true      # Уведомлять о новых заявках на верификацию
    support: true          # Уведомлять о новых обращениях в поддержку
    new-users: false       # Уведомлять о новых пользователях (на будущее)
    errors: true           # Уведомлять об ошибках (на будущее)

server:
  shutdown: graceful  # 🔥 Включаем graceful shutdown
  port: 443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12   # если файл в resources, или используйте полный путь file:/путь/keystore.p12
    key-store-password: u0740562149       # пароль, который вводили при создании keystore
    key-store-type: PKCS12
    key-alias: myalias                    # alias, который задавали в keytool
# Cloudpub URL
app:
  base-url: https://obdurately-benign-mongoose.cloudpub.ru/

spring:
  application:
    name: tcmatch
  lifecycle:
    timeout-per-shutdown-phase: 30s  # 🔥 Таймаут для graceful shutdown

  datasource:
    url: jdbc:h2:mem:tcmatchdb
    username: sa
    password: 123
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console

jpa:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    com.tcmatch: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

telegram:
  bot:
    username: ${BOT_USERNAME:TCMatchBot}
    token: ${BOT_TOKEN:000000000}

yoomoney:
  shopId: 1218176
  secretKey: test_BSMXFx9KeUJjYTZdOn2L8yfjAVjTpvW5DCIHIOJYrzI
  returnUrl: https://obdurately-benign-mongoose.cloudpub.ru/payment/success
  notificationUrl: https://obdurately-benign-mongoose.cloudpub.ru/api/v1/payment/notify